name: Tier 3 No-Install Verification v1 (Retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Historical verifier retired
        shell: bash
        run: |
          set -euo pipefail
          echo "Tier 3 No-Install Verification v1 is retired. Use v2."
