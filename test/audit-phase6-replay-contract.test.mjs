import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const inv=JSON.parse(fs.readFileSync(new URL('./fixtures/audit-phase6/FIXTURE_INVENTORY_v2.json',import.meta.url),'utf8'));test('Phase 6 replay inventory remains 16 owned fixtures',()=>{assert.equal(inv.fixtureOwner,'CurveYield');assert.equal(inv.fileCount,16);assert.equal(inv.fixtures.length,16);});
