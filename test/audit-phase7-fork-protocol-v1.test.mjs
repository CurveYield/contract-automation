import test from 'node:test';
import assert from 'node:assert/strict';
async function protocol(){ try{return await import('../packages/audit-fork-protocol/src/index.mjs');}catch(cause){assert.fail(`Phase 7 protocol module unavailable: ${cause.code}`);} }
const ids={tenantId:'ten_'+'1'.repeat(32),workspaceId:'ws_'+'2'.repeat(32),campaignId:'cmp_'+'3'.repeat(32),forkId:'fork_'+'4'.repeat(32),attemptId:'att_'+'5'.repeat(32)};
function request(){return {schemaVersion:'fork-request-v1',...ids,profileId:'free-development-v1',policyVersion:'fork-policy-v1',requesterId:'usr_alice',scopes:['audit:read','audit:submit'],chainId:1,blockNumber:21000000,blockHash:'0x'+'a'.repeat(64),adapterKind:'external',executionGate:'awaiting_executor',createdAt:'2026-08-01T00:00:00.000Z',idempotencyKey:'fork-create-1'};}
test('validates exact immutable fork identity and canonical digest',async()=>{const p=await protocol(); const value=p.validateForkRequest(request()); assert.equal(value.chainId,1); assert.match(await p.digestForkRequest(value),/^[0-9a-f]{64}$/); assert.equal(p.canonicalJson(value),p.canonicalJson(structuredClone(value)));});
test('rejects mutable block tags, unknown fields, accessors, cycles, and unsafe integers',async()=>{const p=await protocol(); assert.throws(()=>p.validateForkRequest({...request(),blockTag:'latest'}),{code:'unknown_field'}); const accessor={...request()}; Object.defineProperty(accessor,'chainId',{enumerable:true,get(){return 1;}}); assert.throws(()=>p.validateForkRequest(accessor),{code:'unsafe_object'}); const cyclic=request(); cyclic.self=cyclic; assert.throws(()=>p.validateForkRequest(cyclic),{code:'cyclic_value'}); assert.throws(()=>p.validateForkRequest({...request(),blockNumber:Number.MAX_SAFE_INTEGER+1}),{code:'invalid_integer'});});
