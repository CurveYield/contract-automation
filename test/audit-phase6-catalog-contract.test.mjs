import test from 'node:test';import assert from 'node:assert/strict';import {createPhase6ToolCatalog} from '../packages/audit-phase6-tool-catalog/src/index.mjs';test('Phase 6 catalog integration is exact and disabled',()=>{const c=createPhase6ToolCatalog();assert.equal(c.entries.length,3);assert.equal(c.entries.every(x=>x.executionEnabled===false&&x.runnable===false&&x.executor.available===false),true);});
