import { DIRECT_MODE_ID, CONTROL_BRANCH, exactKeys, canonicalJson, sha256, frozenClone, fail, digest } from '../../audit-github-direct-protocol/src/index.mjs';
import { ledgerPath } from './paths.mjs';
const contentBlobSha=(content)=>sha256(canonicalJson(content)).slice(7,47);
export const blobSha=(value,path)=>{if(typeof value!=='string'||!/^[0-9a-f]{40}$/.test(value))fail('invalid_blob_sha',path);return value;};
export function planImmutableCreate(input){const v=exactKeys(input,['path','content'],'$'),path=ledgerPath(v.path),content=frozenClone(v.content),contentDigest=sha256(content);return frozenClone({schemaVersion:'github-direct-ledger-mutation-v1',modeId:DIRECT_MODE_ID,branch:CONTROL_BRANCH,operation:'create-immutable',path,content,contentDigest,expectedBlobSha:null,nextContentBlobSha:contentBlobSha(content),sideEffects:false,usesPrefixListing:false});}
export function planCasUpdate(input){const v=exactKeys(input,['path','content','currentBlobSha','expectedBlobSha'],'$'),path=ledgerPath(v.path),currentBlobSha=blobSha(v.currentBlobSha,'$.currentBlobSha'),expectedBlobSha=blobSha(v.expectedBlobSha,'$.expectedBlobSha');if(currentBlobSha!==expectedBlobSha)fail('stale_blob_sha','$.expectedBlobSha');const content=frozenClone(v.content),contentDigest=sha256(content);return frozenClone({schemaVersion:'github-direct-ledger-mutation-v1',modeId:DIRECT_MODE_ID,branch:CONTROL_BRANCH,operation:'update-cas',path,content,contentDigest,expectedBlobSha,nextContentBlobSha:contentBlobSha(content),sideEffects:false,usesPrefixListing:false});}
export function validateLedgerMutation(input){const v=exactKeys(input,['schemaVersion','modeId','branch','operation','path','content','contentDigest','expectedBlobSha','nextContentBlobSha','sideEffects','usesPrefixListing'],'$');if(v.schemaVersion!=='github-direct-ledger-mutation-v1')fail('invalid_schema','$.schemaVersion');if(v.modeId!==DIRECT_MODE_ID)fail('invalid_mode','$.modeId');if(v.branch!==CONTROL_BRANCH)fail('invalid_control_branch','$.branch');if(!['create-immutable','update-cas'].includes(v.operation))fail('invalid_operation','$.operation');const path=ledgerPath(v.path),content=frozenClone(v.content);digest(v.contentDigest,'$.contentDigest');if(v.contentDigest!==sha256(content))fail('digest_mismatch','$.contentDigest');blobSha(v.nextContentBlobSha,'$.nextContentBlobSha');if(v.nextContentBlobSha!==contentBlobSha(content))fail('blob_sha_mismatch','$.nextContentBlobSha');if(v.operation==='create-immutable'&&v.expectedBlobSha!==null)fail('create_only_violation','$.expectedBlobSha');if(v.operation==='update-cas')blobSha(v.expectedBlobSha,'$.expectedBlobSha');if(v.sideEffects!==false)fail('side_effect_violation','$.sideEffects');if(v.usesPrefixListing!==false)fail('prefix_listing_violation','$.usesPrefixListing');return frozenClone({...v,path,content});}
