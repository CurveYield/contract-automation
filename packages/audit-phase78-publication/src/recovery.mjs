import {validateServiceRequest,exactKeys,digestArray,identifier,sha256,frozenClone,fail} from '../../audit-phase78-service/src/index.mjs';
export function planPublicationRecovery({request:requestInput,plannedDigests,completedDigests,failedStep}){const request=validateServiceRequest(requestInput),planned=digestArray(plannedDigests,'$.plannedDigests',128),completed=digestArray(completedDigests,'$.completedDigests',128),failed=identifier(failedStep,'$.failedStep');for(const item of completed)if(!planned.includes(item))fail('recovery_conflict','$.completedDigests');const remaining=planned.filter(item=>!completed.includes(item));const core={schemaVersion:'audit-phase9-publication-recovery-v1',requestId:request.requestId,requestDigest:request.requestDigest,plannedDigests:planned,completedDigests:completed,remainingDigests:remaining,failedStep:failed,retryPointerLast:true,retrySafe:true,maxAttempts:4};const recoveryDigest=sha256(core);return frozenClone({...core,recoveryId:`publication-recovery-${recoveryDigest.slice(7,31)}`,recoveryDigest});}
