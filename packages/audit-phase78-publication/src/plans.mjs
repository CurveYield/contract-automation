import {validateServiceRequest,exactKeys,denseArray,identifier,digest,integer,sha256,frozenClone,canonicalClone,fail} from '../../audit-phase78-service/src/index.mjs';
import {enforcePublicationQuota} from './quota.mjs';
function keyFor(request,kind,id){return `tenants/${request.tenantId}/workspaces/${request.workspaceId}/${kind}/${id}-v1.json`;}
function finalize(kind,body){const core={schemaVersion:`audit-phase9-${kind}-publication-plan-v1`,...body};const planDigest=sha256(core);return frozenClone({...core,planId:`${kind}-plan-${planDigest.slice(7,31)}`,planDigest});}
export function planImmutablePublication({request:requestInput,records}){const request=validateServiceRequest(requestInput);const items=denseArray(records,'$.records',64).map((entry,i)=>{const v=exactKeys(entry,['kind','id','digest','bytes','retentionDays'],`$.records[${i}]`);return{kind:identifier(v.kind,`$.records[${i}].kind`),id:identifier(v.id,`$.records[${i}].id`),digest:digest(v.digest,`$.records[${i}].digest`),bytes:integer(v.bytes,`$.records[${i}].bytes`,1),retentionDays:integer(v.retentionDays,`$.records[${i}].retentionDays`,1,90)};}).sort((a,b)=>`${a.kind}\0${a.id}`.localeCompare(`${b.kind}\0${b.id}`));if(new Set(items.map(x=>`${x.kind}:${x.id}`)).size!==items.length)fail('duplicate_identity','$.records');const bytes=items.reduce((sum,x)=>sum+x.bytes,0),retentionDays=Math.max(...items.map(x=>x.retentionDays));enforcePublicationQuota({records:items.length,bytes,retentionDays,checkpoints:0,exports:0,pageSize:1});const preconditions=items.map(item=>({class:'class-b',method:'head',key:keyFor(request,item.kind,item.id),contentDigest:item.digest,mustBeAbsentOrExact:true}));const operations=items.map(item=>({class:'class-a',method:'put',key:keyFor(request,item.kind,item.id),digest:item.digest,contentDigest:item.digest,bytes:item.bytes,retentionDays:item.retentionDays,immutable:true,ifNoneMatch:'*'}));return finalize('immutable',{requestId:request.requestId,requestDigest:request.requestDigest,tenantId:request.tenantId,workspaceId:request.workspaceId,preconditions,operations,summary:{classA:items.length,classB:items.length,free:0,bytes,records:items.length},usesPrefixListing:false,retrySafe:true});}
export function planMutablePointerPublication({request:requestInput,current,pointer}){const request=validateServiceRequest(requestInput),c=exactKeys(current,['version','etag'],'$.current'),p=exactKeys(pointer,['kind','id','digest','bytes'],'$.pointer');const version=integer(c.version,'$.current.version',0),etag=digest(c.etag,'$.current.etag');if(request.expectedVersion===null||request.expectedVersion!==version||request.expectedEtag!==etag)fail('stale_state','$.current');const nextVersion=version+1,kind=identifier(p.kind,'$.pointer.kind'),id=identifier(p.id,'$.pointer.id'),pointerDigest=digest(p.digest,'$.pointer.digest'),bytes=integer(p.bytes,'$.pointer.bytes',1,20_000_000);const operationCore={class:'class-a',method:'put',key:keyFor(request,kind,id),contentDigest:pointerDigest,bytes,immutable:false,ifMatch:etag,nextVersion};const operation={...operationCore,digest:sha256(operationCore)};return finalize('mutable-pointer',{requestId:request.requestId,requestDigest:request.requestDigest,tenantId:request.tenantId,workspaceId:request.workspaceId,currentVersion:version,nextVersion,operations:[operation],summary:{classA:1,classB:0,free:0,bytes,records:1},usesPrefixListing:false,retrySafe:true});}
export function validatePublicationPlan(input){if(!input||typeof input!=='object'||Array.isArray(input))fail('invalid_plan','$');const copy=canonicalClone(input),digestValue=copy.planDigest,id=copy.planId;delete copy.planDigest;delete copy.planId;const expected=sha256(copy),prefix=copy.schemaVersion.includes('immutable-')?'immutable':copy.schemaVersion.includes('mutable-pointer-')?'mutable-pointer':null;if(!prefix)fail('invalid_schema','$.schemaVersion');if(digestValue!==expected)fail('digest_mismatch','$.planDigest');if(id!==`${prefix}-plan-${expected.slice(7,31)}`)fail('identity_mismatch','$.planId');return frozenClone(input);}
