import { exactKeys,identifier,boundedString,integer,digest,booleanValue,timestamp,sha256,frozenClone,fail } from '../../audit-github-direct-protocol/src/index.mjs';
export function createArtifactMetadata(input){const v=exactKeys(input,['artifactId','name','sizeBytes','digest','expired','createdAt','expiresAt'],'$');const body={schemaVersion:'github-direct-artifact-metadata-v1',artifactId:identifier(v.artifactId,'$.artifactId'),name:boundedString(v.name,'$.name',256),sizeBytes:integer(v.sizeBytes,'$.sizeBytes',0,2_000_000_000),digest:digest(v.digest,'$.digest'),expired:booleanValue(v.expired,'$.expired'),createdAt:timestamp(v.createdAt,'$.createdAt'),expiresAt:timestamp(v.expiresAt,'$.expiresAt')};if(body.expiresAt<body.createdAt)fail('invalid_expiry','$.expiresAt');const metadataDigest=sha256(body);return frozenClone({...body,metadataDigest});}
export function validateArtifactMetadata(input){const v=exactKeys(input,['schemaVersion','artifactId','name','sizeBytes','digest','expired','createdAt','expiresAt','metadataDigest'],'$');if(v.schemaVersion!=='github-direct-artifact-metadata-v1')fail('invalid_schema','$.schemaVersion');const rebuilt=createArtifactMetadata({artifactId:v.artifactId,name:v.name,sizeBytes:v.sizeBytes,digest:v.digest,expired:v.expired,createdAt:v.createdAt,expiresAt:v.expiresAt});if(v.metadataDigest!==rebuilt.metadataDigest)fail('digest_mismatch','$.metadataDigest');return rebuilt;}
