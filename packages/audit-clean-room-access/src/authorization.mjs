import { exactKeys,identifier,timestamp,enumValue,stringArray,frozenClone,fail,validateCampaignAccessContext } from '../../audit-clean-room-protocol/src/index.mjs';
import { ACCESS_DECISION_SCHEMA,RESOURCE_KINDS,REQUIRED_SCOPES,REASONS,READABLE_STATES_BY_ROLE } from './constants.mjs';
const hasAll=(actual,required)=>required.every((scope)=>actual.includes(scope));
export function readAuthorizationReason(context){if(!context.scopes.includes('campaign:read'))return 'scope_missing';if(!READABLE_STATES_BY_ROLE[context.campaignRole]?.includes(context.campaignState))return 'role_state_denied';return 'allowed';}
export function authorizeCampaignAccess(contextInput,requestInput){const context=validateCampaignAccessContext(contextInput);const r=exactKeys(requestInput,['tenantId','workspaceId','campaignId','requiredScopes','resourceKind','at'],'$');const request={tenantId:identifier(r.tenantId,'$.tenantId'),workspaceId:identifier(r.workspaceId,'$.workspaceId'),campaignId:identifier(r.campaignId,'$.campaignId'),requiredScopes:stringArray(r.requiredScopes,'$.requiredScopes',{maximum:8,item:(x,p)=>enumValue(x,REQUIRED_SCOPES,p)}),resourceKind:enumValue(r.resourceKind,RESOURCE_KINDS,'$.resourceKind'),at:timestamp(r.at,'$.at')};let reason='allowed';if(context.tenantId!==request.tenantId)reason='tenant_mismatch';else if(context.workspaceId!==request.workspaceId)reason='workspace_mismatch';else if(context.campaignId!==request.campaignId)reason='campaign_mismatch';else if(!hasAll(context.scopes,request.requiredScopes))reason='scope_missing';else if(request.requiredScopes.includes('campaign:read'))reason=readAuthorizationReason(context);return frozenClone({schemaVersion:ACCESS_DECISION_SCHEMA,allowed:reason==='allowed',reason,tenantId:context.tenantId,workspaceId:context.workspaceId,campaignId:context.campaignId,requesterId:context.requesterId,policyId:context.policyId,resourceKind:request.resourceKind,decidedAt:request.at});}
export function validateAccessDecision(input){const v=exactKeys(input,['schemaVersion','allowed','reason','tenantId','workspaceId','campaignId','requesterId','policyId','resourceKind','decidedAt'],'$');if(v.schemaVersion!==ACCESS_DECISION_SCHEMA)fail('invalid_schema','$.schemaVersion');if(typeof v.allowed!=='boolean')fail('invalid_boolean','$.allowed');enumValue(v.reason,REASONS,'$.reason');if(v.allowed!==(v.reason==='allowed'))fail('decision_contradiction','$.allowed');identifier(v.tenantId,'$.tenantId');identifier(v.workspaceId,'$.workspaceId');identifier(v.campaignId,'$.campaignId');identifier(v.requesterId,'$.requesterId');identifier(v.policyId,'$.policyId');enumValue(v.resourceKind,RESOURCE_KINDS,'$.resourceKind');timestamp(v.decidedAt,'$.decidedAt');return frozenClone(v);}
