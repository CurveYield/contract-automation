import { exactKeys,identifier,digest,timestamp,boundedString,sha256,frozenClone,fail } from './boundary.mjs';
import { SHARE_GRANT_SCHEMA,SHARE_REVOCATION_SCHEMA } from './constants.mjs';
export { SHARE_GRANT_SCHEMA,SHARE_REVOCATION_SCHEMA } from './constants.mjs';
export function createShareGrant(input){const v=exactKeys(input,['tenantId','workspaceId','sourceCampaignId','targetCampaignId','artifactId','artifactDigest','sourceDigest','issuedAt','expiresAt'],'$');const body={schemaVersion:SHARE_GRANT_SCHEMA,tenantId:identifier(v.tenantId,'$.tenantId'),workspaceId:identifier(v.workspaceId,'$.workspaceId'),sourceCampaignId:identifier(v.sourceCampaignId,'$.sourceCampaignId'),targetCampaignId:identifier(v.targetCampaignId,'$.targetCampaignId'),artifactId:identifier(v.artifactId,'$.artifactId'),artifactDigest:digest(v.artifactDigest,'$.artifactDigest'),sourceDigest:digest(v.sourceDigest,'$.sourceDigest'),issuedAt:timestamp(v.issuedAt,'$.issuedAt'),expiresAt:timestamp(v.expiresAt,'$.expiresAt')};if(body.sourceCampaignId===body.targetCampaignId)fail('invalid_grant','$.targetCampaignId');if(body.expiresAt<=body.issuedAt)fail('invalid_expiry','$.expiresAt');const grantDigest=sha256(body);return frozenClone({...body,grantId:`grant-${grantDigest.slice(7,31)}`,grantDigest});}
export function validateShareGrant(input){const v=exactKeys(input,['schemaVersion','grantId','grantDigest','tenantId','workspaceId','sourceCampaignId','targetCampaignId','artifactId','artifactDigest','sourceDigest','issuedAt','expiresAt'],'$');if(v.schemaVersion!==SHARE_GRANT_SCHEMA)fail('invalid_schema','$.schemaVersion');const rebuilt=createShareGrant({tenantId:v.tenantId,workspaceId:v.workspaceId,sourceCampaignId:v.sourceCampaignId,targetCampaignId:v.targetCampaignId,artifactId:v.artifactId,artifactDigest:v.artifactDigest,sourceDigest:v.sourceDigest,issuedAt:v.issuedAt,expiresAt:v.expiresAt});if(v.grantId!==rebuilt.grantId)fail('identity_mismatch','$.grantId');if(v.grantDigest!==rebuilt.grantDigest)fail('digest_mismatch','$.grantDigest');return rebuilt;}
export function createShareGrantRevocation(input){const v=exactKeys(input,['grantId','grantDigest','revokedAt','reasonCode'],'$');const body={schemaVersion:SHARE_REVOCATION_SCHEMA,grantId:identifier(v.grantId,'$.grantId'),grantDigest:digest(v.grantDigest,'$.grantDigest'),revokedAt:timestamp(v.revokedAt,'$.revokedAt'),reasonCode:boundedString(v.reasonCode,'$.reasonCode',64)};const revocationDigest=sha256(body);return frozenClone({...body,revocationId:`revoke-${revocationDigest.slice(7,31)}`,revocationDigest});}
export function validateShareGrantRevocation(input){const v=exactKeys(input,['schemaVersion','revocationId','revocationDigest','grantId','grantDigest','revokedAt','reasonCode'],'$');if(v.schemaVersion!==SHARE_REVOCATION_SCHEMA)fail('invalid_schema','$.schemaVersion');const rebuilt=createShareGrantRevocation({grantId:v.grantId,grantDigest:v.grantDigest,revokedAt:v.revokedAt,reasonCode:v.reasonCode});if(v.revocationId!==rebuilt.revocationId)fail('identity_mismatch','$.revocationId');if(v.revocationDigest!==rebuilt.revocationDigest)fail('digest_mismatch','$.revocationDigest');return rebuilt;}
