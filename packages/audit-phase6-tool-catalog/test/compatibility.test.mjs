import test from 'node:test';import assert from 'node:assert/strict';
import {createPhase6ToolResultEnvelope} from '../../audit-phase6-result-contracts/src/index.mjs';import {assertPhase6PackageCompatibility} from '../src/index.mjs';import {resultFor} from '../../audit-phase6-result-contracts/test/helpers.mjs';

test('asserts exact profile/template/parser/catalog/result compatibility',()=>{const results=[createPhase6ToolResultEnvelope('solidity-smt-v1',resultFor('solidity-smt-proof-v2.json')),createPhase6ToolResultEnvelope('halmos-v1',resultFor('halmos-counterexample-v2.json')),createPhase6ToolResultEnvelope('formal-obligations-v1',resultFor('formal-obligations-proof-v2.json'))];const c=assertPhase6PackageCompatibility({results});assert.equal(c.compatible,true);assert.equal(c.checkedResults,3);assert.equal(Object.isFrozen(c),true);});

test('one-field substitutions fail with stable codes and paths',()=>{const e=createPhase6ToolResultEnvelope('solidity-smt-v1',resultFor('solidity-smt-proof-v2.json'));const mutations=[['profileId','halmos-v1'],['parserId','halmos-parser-v1'],['toolVersion','0.3.3'],['captureSchemaVersion','halmos-capture-v1'],['resultSchemaVersion','other'],['trustedProducer','other']];for(const [k,v] of mutations){const x=structuredClone(e);x[k]=v;assert.throws(()=>assertPhase6PackageCompatibility({results:[x]}),er=>er.code==='identity_mismatch'&&er.path===`$.results[0].${k}`);}});
