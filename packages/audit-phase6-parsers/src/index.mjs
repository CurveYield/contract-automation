import {
  PHASE6_BOUNDS,
  Phase6ValidationError,
  sanitizeValidationPath,
  validateFormalResult
} from '../../audit-phase6-profile-contracts/src/index.mjs';

const CAPTURE_SCHEMAS = Object.freeze({
  'solidity-smt-v1': 'solidity-smt-capture-v1',
  'halmos-v1': 'halmos-capture-v1',
  'formal-obligations-v1': 'formal-obligations-capture-v1'
});
const TOOL_VERSIONS = Object.freeze({
  'solidity-smt-v1': '0.8.30',
  'halmos-v1': '0.3.3',
  'formal-obligations-v1': '1.0.0'
});
export const PHASE6_TRUSTED_PRODUCER = 'curveyield-formal-capture-producer-v1';
const LIMITS = Object.freeze({obligations:PHASE6_BOUNDS.obligations,assertions:PHASE6_BOUNDS.assertions,models:PHASE6_BOUNDS.models,traces:PHASE6_BOUNDS.traces,counterexamples:PHASE6_BOUNDS.counterexamples,diagnostics:PHASE6_BOUNDS.diagnostics,sourceReferences:PHASE6_BOUNDS.sourceReferences,parserWarnings:PHASE6_BOUNDS.parserWarnings});
const KEYS = new Set(['schemaVersion','trustedProducer','profileId','toolVersion','outcome','obligations','assertions','models','traces','counterexamples','diagnostics','sourceReferences','parserWarnings','truncated']);
function empty(profileId,outcome='parser_error'){return{schemaVersion:'formal-result-v1',profileId,outcome,obligations:[],assertions:[],models:[],traces:[],counterexamples:[],diagnostics:[],sourceReferences:[],parserWarnings:[],truncated:false};}
function parserError(profileId,code,path='$'){const safe=/^[A-Za-z][A-Za-z0-9_.:-]{0,79}$/.test(code)?code:'unexpected_parser_error';const result=empty(profileId);result.diagnostics=[{code:safe,severity:'error',message:`Inert capture parsing failed with ${safe}`,sourceReferenceIds:[]}];result.parserWarnings=[{code:'capture_rejected',message:'Capture rejected at a bounded validation path',path:sanitizeValidationPath(path)}];try{return validateFormalResult(result);}catch{return{...empty(profileId),diagnostics:[{code:'unexpected_parser_error',severity:'error',message:'Inert capture parsing failed with unexpected_parser_error',sourceReferenceIds:[]}],parserWarnings:[{code:'capture_rejected',message:'Capture rejected at a bounded validation path',path:'$'}]};}}
function stable(value){if(Array.isArray(value))return`[${value.map(stable).join(',')}]`;if(value&&typeof value==='object')return`{${Object.keys(value).sort().map(k=>`${JSON.stringify(k)}:${stable(value[k])}`).join(',')}}`;return JSON.stringify(value);}
function identity(key,item){if(key==='diagnostics')return`${item?.code??''}\0${item?.severity??''}\0${stable(item?.sourceReferenceIds??[])}`;if(key==='parserWarnings')return`${item?.code??''}\0${item?.path??''}`;return String(item?.id??'');}
function bounded(source,key,limit,path){if(!Array.isArray(source))throw new Phase6ValidationError('invalid_collection',`${path} must be an array`,path);const map=new Map();for(const item of source){const id=identity(key,item),encoded=stable(item);if(map.has(id)&&map.get(id).encoded!==encoded)throw new Phase6ValidationError('conflicting_duplicate',`${path} contains conflicting duplicate identities`,path);if(!map.has(id))map.set(id,{item,id,encoded});}const records=[...map.values()].sort((a,b)=>a.id.localeCompare(b.id)||a.encoded.localeCompare(b.encoded));return{items:records.slice(0,limit).map(x=>structuredClone(x.item)),truncated:records.length>limit};}
function parseBytes(bytes,profileId){if(!(bytes instanceof Uint8Array))throw new TypeError('parser input must be an explicitly supplied Uint8Array of inert bytes');try{if(bytes.byteLength>PHASE6_BOUNDS.inputBytes)return parserError(profileId,'input_too_large');let text;try{text=new TextDecoder('utf-8',{fatal:true}).decode(bytes);}catch{return parserError(profileId,'invalid_utf8');}let capture;try{capture=JSON.parse(text);}catch{return parserError(profileId,'invalid_json');}try{if(!capture||typeof capture!=='object'||Array.isArray(capture)||![Object.prototype,null].includes(Object.getPrototypeOf(capture)))throw new Phase6ValidationError('invalid_plain_object','capture must be an ordinary object','$');for(const key of Object.keys(capture))if(!KEYS.has(key))throw new Phase6ValidationError('unknown_field','capture contains a rejected field','$.[rejected-field]');for(const key of KEYS)if(!Object.hasOwn(capture,key))throw new Phase6ValidationError('missing_field',`capture is missing ${key}`,`$.${key}`);if(capture.schemaVersion!==CAPTURE_SCHEMAS[profileId])throw new Phase6ValidationError('invalid_capture_schema','capture schema mismatch','$.schemaVersion');if(capture.profileId!==profileId)throw new Phase6ValidationError('invalid_profile_id','capture profile mismatch','$.profileId');if(capture.trustedProducer!==PHASE6_TRUSTED_PRODUCER)throw new Phase6ValidationError('invalid_trusted_producer','capture producer is not allowlisted','$.trustedProducer');if(capture.toolVersion!==TOOL_VERSIONS[profileId])throw new Phase6ValidationError('invalid_tool_version','capture tool version mismatch','$.toolVersion');const result=empty(profileId,capture.outcome),warnings=[];for(const[key,limit]of Object.entries(LIMITS)){const normalized=bounded(capture[key],key,limit,`$.${key}`);result[key]=normalized.items;if(normalized.truncated)warnings.push({code:'collection_truncated',message:`${key} was truncated to the profile bound`,path:`$.${key}`});}if(typeof capture.truncated!=='boolean')throw new Phase6ValidationError('invalid_type','$.truncated must be boolean','$.truncated');for(const model of result.models){if(Array.isArray(model.entries)&&model.entries.length>PHASE6_BOUNDS.modelEntries){model.entries=model.entries.slice(0,PHASE6_BOUNDS.modelEntries);warnings.push({code:'collection_truncated',message:'Model entries were truncated to the profile bound',path:'$.models.entries'});}}for(const trace of result.traces){if(Array.isArray(trace.steps)&&trace.steps.length>PHASE6_BOUNDS.traceDepth){trace.steps=trace.steps.slice(0,PHASE6_BOUNDS.traceDepth);warnings.push({code:'collection_truncated',message:'Trace steps were truncated to the profile bound',path:'$.traces.steps'});}}result.parserWarnings=[...result.parserWarnings,...warnings].slice(0,PHASE6_BOUNDS.parserWarnings);result.truncated=capture.truncated||warnings.length>0;return validateFormalResult(result);}catch(error){return parserError(profileId,error instanceof Phase6ValidationError?error.code:'unexpected_parser_error',error?.path);}}catch{return parserError(profileId,'unexpected_parser_error');}}
export function parseSoliditySmtBytes(bytes){return parseBytes(bytes,'solidity-smt-v1');}
export function parseHalmosBytes(bytes){return parseBytes(bytes,'halmos-v1');}
export function parseFormalObligationsBytes(bytes){return parseBytes(bytes,'formal-obligations-v1');}
