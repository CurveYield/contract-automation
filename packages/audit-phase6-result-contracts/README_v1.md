# CurveYield Phase 6 Result Contracts v1

Strict deterministic envelopes for accepted `formal-result-v1` outputs from the three Phase 6 parser functions. The runtime validator binds profile, parser package/function, capture schema, tool version, trusted producer, outcome, exact evidence, derived counts, and truncation state. It returns recursively frozen defensive canonical clones and exposes no execution capability.
