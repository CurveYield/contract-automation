import { createCleanRoomViewModel, createForkViewModel } from '../../../packages/audit-report-view-model/src/index.mjs';
import { escapeHtml, renderState } from './render.mjs';
import { readonlyNotice, renderIdentifier, renderLink, statusBadge, textOrDash } from './page-helpers-v1.mjs';

export function renderForkPage(input) {
  const fork = createForkViewModel(input);
  const checkpoints = fork.checkpoints.length ? `<div class="table-scroll" tabindex="0" aria-label="Scrollable checkpoint table"><table><caption>Checkpoints</caption><thead><tr><th scope="col">Checkpoint</th><th scope="col">Status</th><th scope="col">Created</th><th scope="col">Export</th></tr></thead><tbody>${fork.checkpoints.map((checkpoint) => `<tr><th scope="row">${escapeHtml(checkpoint.label)}<div>${renderIdentifier(checkpoint.id)}</div></th><td>${statusBadge(checkpoint.status)}</td><td>${textOrDash(checkpoint.createdAt)}</td><td>${checkpoint.exportUrl ? renderLink(checkpoint.exportUrl, 'Open export') : '—'}</td></tr>`).join('')}</tbody></table></div>` : renderState({ kind: 'empty', message: 'No checkpoints are visible.' });
  const exports = fork.exports.length ? `<div class="table-scroll" tabindex="0" aria-label="Scrollable export table"><table><caption>Exports</caption><thead><tr><th scope="col">Export</th><th scope="col">Status</th><th scope="col">Checkpoint</th><th scope="col">Size</th></tr></thead><tbody>${fork.exports.map((item) => `<tr><th scope="row">${renderLink(item.url, item.label)}<div>${renderIdentifier(item.id)}</div></th><td>${statusBadge(item.status)}</td><td>${item.checkpointId ? renderIdentifier(item.checkpointId) : '—'}</td><td>${item.sizeBytes} bytes</td></tr>`).join('')}</tbody></table></div>` : renderState({ kind: 'empty', message: 'No exports are visible.' });
  return `${readonlyNotice()}<section aria-labelledby="fork-summary"><h2 id="fork-summary">Persistent fork summary</h2><dl><div><dt>Identifier</dt><dd>${renderIdentifier(fork.id)}</dd></div><div><dt>Status</dt><dd>${statusBadge(fork.status, fork.stateLabel)}</dd></div><div><dt>Export status</dt><dd>${statusBadge(fork.exportStatus)}</dd></div><div><dt>Restore status</dt><dd>${statusBadge(fork.restoreStatus)}</dd></div><div><dt>Delete status</dt><dd>${statusBadge(fork.deleteStatus)}</dd></div><div><dt>Tombstone status</dt><dd>${statusBadge(fork.tombstoneStatus)}</dd></div><div><dt>Created</dt><dd>${textOrDash(fork.createdAt)}</dd></div><div><dt>Retention expires</dt><dd>${textOrDash(fork.retentionExpiresAt)}</dd></div></dl></section><section aria-labelledby="checkpoints-heading"><h2 id="checkpoints-heading">Checkpoints</h2>${checkpoints}</section><section aria-labelledby="exports-heading"><h2 id="exports-heading">Exports</h2>${exports}</section>`;
}

export function renderCleanRoomPage(input) {
  const campaign = createCleanRoomViewModel(input);
  const merges = campaign.merges.length ? `<ol>${campaign.merges.map((merge) => `<li><strong>${escapeHtml(merge.label || merge.id)}</strong> ${statusBadge(merge.status)}<div>${renderIdentifier(merge.id)}</div></li>`).join('')}</ol>` : renderState({ kind: 'empty', message: 'No controlled merges are visible.' });
  const provenance = campaign.provenance.length ? `<div class="table-scroll" tabindex="0" aria-label="Scrollable provenance table"><table><caption>Visible provenance</caption><thead><tr><th scope="col">Source</th><th scope="col">Type</th><th scope="col">Revision</th><th scope="col">Report</th></tr></thead><tbody>${campaign.provenance.map((item) => `<tr><th scope="row">${escapeHtml(item.label)}<div>${renderIdentifier(item.sourceId)}</div></th><td>${escapeHtml(item.sourceType)}</td><td>${renderIdentifier(item.commitSha)}</td><td>${item.reportId ? `<a href="/reports/${encodeURIComponent(item.reportId)}">View report</a>` : '—'}</td></tr>`).join('')}</tbody></table></div>` : renderState({ kind: 'empty', message: 'No visible provenance is available.' });
  return `${readonlyNotice()}<section aria-labelledby="clean-summary"><h2 id="clean-summary">Clean-room summary</h2><dl><div><dt>Identifier</dt><dd>${renderIdentifier(campaign.id)}</dd></div><div><dt>Status</dt><dd>${statusBadge(campaign.status)}</dd></div><div><dt>Access status</dt><dd>${statusBadge(campaign.accessStatus)}</dd></div><div><dt>Share status</dt><dd>${statusBadge(campaign.shareStatus)}</dd></div><div><dt>Updated</dt><dd>${textOrDash(campaign.updatedAt)}</dd></div></dl></section><section aria-labelledby="merges-heading"><h2 id="merges-heading">Controlled merges</h2>${merges}</section><section aria-labelledby="provenance-heading"><h2 id="provenance-heading">Provenance</h2>${provenance}</section>`;
}
