{
  "rules": [
    {
      "allowed": {
        "origins": ["https://audit.preflight.curveyield.online"],
        "methods": ["GET", "HEAD", "PUT"],
        "headers": ["content-type", "if-match", "if-none-match"]
      },
      "exposeHeaders": ["etag"],
      "maxAgeSeconds": 3600
    }
  ]
}
